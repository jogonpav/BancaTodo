export class User {
  id: number;
  userName: string;
  password: string;
  jwt: string;
  lastName: string;
  firstName: string;

/*   constructor(
    userName: string,
    password: string,
    jwt: string,
    lastName: string,
    firstName: string){
      this.userName = userName;
      this.password = password;
      this.jwt = jwt;
      this.lastName = lastName;
      this.firstName = firstName;
  }  */
}
